import React from 'react';
import { Link } from 'react-router-dom';

/**
 * Footer Component
 * 
 * A simple footer component that provides consistent styling and spacing
 * at the bottom of pages. This component uses the application's theme colors
 * and automatically positions itself at the bottom of the page.
 * 
 * Features:
 * - Consistent styling with the application theme
 * - Automatic positioning at the bottom of the page
 * - Responsive design
 * 
 * @returns {JSX.Element} The rendered footer component
 */
function Footer() {
    return (
        <footer className="bg-[#CBADD8] text-white h-[50px] py-4 mt-auto">
        </footer>
    );
}

export default Footer;